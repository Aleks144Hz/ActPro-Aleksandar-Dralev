@model IEnumerable<ActPro.DAL.Entities.Place>
@{
    ViewData["Title"] = "Търсене";
    var city = Context.Request.Query["city"];
    var activity = Context.Request.Query["activity"];
}

<div class="container-fluid mt-4">
    <div class="row">
        <aside class="col-md-3 mb-4">
            <div class="card shadow-sm border-success">
                <div class="card-body">
                    <h5 class="card-title text-center mb-3">Филтрирай по</h5>

                    <form method="get" asp-controller="Search" asp-action="Index">
                        <input type="hidden" name="city" value="@city" />
                        <input type="hidden" name="activity" value="@activity" />

                        <label class="form-label fw-bold">Цена</label>
                        <div class="d-flex justify-content-between">
                            <input type="number" class="form-control me-1" placeholder="0" name="minPrice">
                            <input type="number" class="form-control ms-1" placeholder="999" name="maxPrice">
                        </div>
                        <hr />

                        <label class="form-label fw-bold">Капацитет</label>
                        <input type="range" class="form-range" min="0" max="30" id="capacityRange">
                        <div class="d-flex justify-content-between text-muted small">
                            <span>0</span>
                            <span>30+</span>
                        </div>
                        <hr />

                        <label class="form-label fw-bold">Работно време</label>
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" id="weekendCheck">
                            <label class="form-check-label" for="weekendCheck">Уикенд</label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" id="weekdayCheck">
                            <label class="form-check-label" for="weekdayCheck">През седмицата</label>
                        </div>

                        <button type="submit" class="btn btn-outline-success w-100 mt-3">Приложи</button>
                    </form>
                </div>
            </div>
        </aside>

        <main class="col-md-9">
            <div class="mb-3 position-relative">
                <img src="https://via.placeholder.com/800x200?text=Карта+на+района" class="img-fluid w-100 rounded" style="object-fit: cover; height: 150px;">
                <button class="btn btn-success btn-sm position-absolute bottom-0 start-50 translate-middle-x mb-2">Показване на картата</button>
            </div>

            <div class="d-flex justify-content-between align-items-center mb-3">
                <h4 class="mb-0">@city: открити @Model.Count() места за @activity</h4>
                <div class="dropdown">
                    <button class="btn btn-outline-success dropdown-toggle" type="button" data-bs-toggle="dropdown">
                        Подреди по
                    </button>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="#">Цена (възходяща)</a></li>
                        <li><a class="dropdown-item" href="#">Рейтинг</a></li>
                        <li><a class="dropdown-item" href="#">Капацитет</a></li>
                    </ul>
                </div>
            </div>

            <div class="d-flex flex-column gap-3">
                @if (!Model.Any())
                {
                    <div class="alert alert-info">Няма намерени резултати за тези критерии.</div>
                }

                @foreach (var place in Model)
                {
                    <div class="card border-success shadow-sm">
                        <div class="row g-0">
                            <div class="col-md-4">
                                <img src="@(place.PlaceImages?.FirstOrDefault()?.ImageUrl ?? "https://via.placeholder.com/300x200")"
                                     class="img-fluid rounded-start h-100" style="object-fit: cover;" alt="@place.Name">
                            </div>
                            <div class="col-md-8">
                                <div class="card-body d-flex flex-column h-100">
                                    <div class="d-flex justify-content-between">
                                        <h5 class="card-title">@place.Name</h5>
                                        <span class="badge bg-warning text-dark">⭐ @place.Rating/10</span>
                                    </div>
                                    <p class="card-text text-muted">@place.Description</p>
                                    <div class="mt-auto d-flex justify-content-between align-items-center">
                                        <h4 class="text-success mb-0">@place.Price лв/ч</h4>
                                        <a asp-controller="Reservation" asp-action="Index" asp-route-id="@place.Id" class="btn btn-success stretched-link">Виж повече</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </main>
    </div>
</div>
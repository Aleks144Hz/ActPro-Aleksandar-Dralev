@using ActPro.Domain.Models.Areas
@model UsersIndexViewModel
@{
    ViewData["Title"] = DomainResources.Users;
}

<div class="container-fluid mt-4">
    <div class="row">
        <div class="col-md-3">
            @await Html.PartialAsync("_AdminMenu")
        </div>
        <div class="col-md-9">
            <h2><i class="bi bi-people"></i> @DomainResources.Users</h2>
            <div class="card shadow-sm">
                <table class="table table-hover align-middle mb-0 admin-table">
                    <thead class="table-dark">
                        <tr>
                            <th>@DomainResources.FirstName</th>
                            <th>@DomainResources.Email</th>
                            <th>@DomainResources.PhoneNumber</th>
                            <th>@DomainResources.Role</th>
                            <th class="text-center no-sort">@DomainResources.Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model.Users)
                        {
                            <tr>
                                <td data-label="@DomainResources.FirstName.ToUpper()"> <div class="fw-bold">@user.FullName</div></td>
                                <td data-label="@DomainResources.Email.ToUpper()"> <div class="fw-bold">@user.Email</div></td>
                                <td data-label="@DomainResources.PhoneNumber.ToUpper()"> <div class="fw-bold">@user.PhoneNumber</div></td>
                                <td data-label="@DomainResources.Actions.ToUpper()" class="text-center">
                                    @if (user.IsAdmin)
                                    {
                                        <span class="badge bg-danger">@DomainResources.Admin</span>
                                    }
                                    @if (user.IsOwner)
                                    {
                                        <span class="badge bg-warning text-dark">@DomainResources.Owner</span>
                                    }
                                    @if (!user.IsAdmin && !user.IsOwner)
                                    {
                                        <span class="badge bg-secondary">@DomainResources.User</span>
                                    }
                                </td>
                                <td>
                                    <div class="d-flex justify-content-center gap-2">
                                        <form asp-action="ToggleAdmin" method="post" class="d-inline">
                                            <input type="hidden" name="userId" value="@user.Id" />
                                            <button type="submit" class="btn btn-sm @(user.IsAdmin ? "btn-outline-danger" : "btn-outline-success")">
                                                <i class="bi @(user.IsAdmin ? "bi-person-dash" : "bi-person-check")"></i> @DomainResources.Admin
                                            </button>
                                        </form>
                                        <form asp-action="ToggleOwner" method="post" class="d-inline">
                                            <input type="hidden" name="userId" value="@user.Id" />
                                            <button type="submit" class="btn btn-sm @(user.IsOwner ? "btn-outline-danger" : "btn-outline-primary")">
                                                <i class="bi bi-shop"></i> @DomainResources.Owner
                                            </button>
                                        </form>
                                        <form asp-action="BanUser" asp-route-userId="@user.Id" method="post" class="d-inline">
                                            <button type="submit" class="btn btn-sm btn-danger confirm-action"
                                                    data-message="@DomainResources.BanUserWarning">
                                                <i class="bi bi-shield-slash"></i> @DomainResources.Ban
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
@section Styles
{
    <link rel="stylesheet" href="~/css/admin.css" asp-append-version="true" />
}